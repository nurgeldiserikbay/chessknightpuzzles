import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thelightcome.chessknightpuzzles',
  appName: 'Chess Knight Puzzles',
  webDir: 'docs',
  server: {
    androidScheme: 'https'
  }
};

export default config;
